# Copy to .env and fill in. NEVER commit .env (it is gitignored).
# Endpoint shared by all experiment conditions (keeps model fixed across C0/C1/C2).
ASTIMATE_LLM_ENDPOINT=https://llm-proxy.datamaker.io
ASTIMATE_LLM_API_KEY=sk-REPLACE-ME
ASTIMATE_LLM_MODEL=bunker-flash
